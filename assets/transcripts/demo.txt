$ pingu status
[pingu] vault: vault   lane: feature   phase: done   (loop closed)
[pingu] autonomy: full-loop — runs the whole lane, then stops once for review
[pingu] tasks: done:3

$ pingu gate verify
[gate] verify   (planned)
  planned        test suite passes
                 would run: .venv/bin/python -m pytest -q
  manual-review  reviews returned with no blocking findings
                 The four reviewers ran and their blocking findings are closed.
[gate] 2 check(s) still outstanding — the gate is not met yet
[gate] manual-review means not verified by tooling; a human confirms those
[gate] re-run with --execute to run the declared commands

$ pingu gate verify --execute
[gate] verify   (executed)
  passed         test suite passes
                 exit code 0
  manual-review  reviews returned with no blocking findings
                 The four reviewers ran and their blocking findings are closed.
[gate] 1 check(s) still outstanding — the gate is not met yet
[gate] manual-review means not verified by tooling; a human confirms those
